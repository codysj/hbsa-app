/**
 * Per-cycle settings. These are the only values that change between
 * recruitment seasons — update them here, not in the pages.
 */

/** Set to true to close applications and show "deadline passed" everywhere. */
export const APPLICATION_CLOSED = true

/** Shown in page titles and closed-application messaging. */
export const SEMESTER = 'Fall 2026'

/** Shown on the landing page CTA. Prose, not a parsed date. */
export const DEADLINE = 'September 20th at 11:59 PM'

/** Options in the graduating-year dropdown on the basic info step. */
export const GRADUATING_YEARS = ['2027', '2028', '2029', '2030']
